# amica-python — Compute Canada benchmark configuration (per-user / per-site).
#
# Copy this file to env.local and edit it for your allocation and paths:
#     cp env.template env.local && nano env.local
# fir_env.sh sources env.local if present, so these values override its defaults.
# env.local is gitignored — your edits stay local.
#
# NOTE: Slurm "#SBATCH" headers (account, partition, --gres) are parsed before the
# job runs and cannot be set from here. The recommended path is `bash submit_all.sh`,
# which reads the values below and passes them on the `sbatch` command line (CLI
# overrides the fir defaults baked into each submit_*_v3.sh). You can still submit a
# single script directly on fir with `sbatch submit_jax_gpu_v3.sh`.

# ── Slurm allocations + GPU hardware (consumed by submit_all.sh) ──
export AMICA_ACCOUNT="def-yourpi"               # CPU jobs   (sbatch --account)
export AMICA_GPU_ACCOUNT="def-yourpi_gpu"       # GPU job    (sbatch --account)
export AMICA_GPU_PARTITION="gpubase_bygpu_b1"   # fir H100 partition; "" if your site needs none
export AMICA_GPU_GRES="gpu:h100:1"              # fir H100; change to your GPU, e.g. gpu:a100:1 / gpu:v100:1
export AMICA_CPU_PARTITION=""                   # most sites need none; set if yours requires it

# ── Data + outputs (override fir_env.sh defaults) ──
export BIDS_ROOT_DS4505="/project/<group>/datasets/openneuro/ds004505/raw_bids"
export AMICA_RESULTS_DIR="/scratch/$USER/amica_python_validation_v3"

# ── MIR replication datasets (ds004504 + ds004621) — consumed by submit_replication.sh ──
# The 2nd + 3rd real datasets (Miltiadous 2023 resting clinical HC; Dzianok 2022 128-ch
# resting). Fetch with download_ds004504.sh / download_ds004621.sh, then submit_replication.sh.
export BIDS_ROOT_DS4504="/project/<group>/datasets/openneuro/ds004504"   # OpenNeuro ds004504 root
export BIDS_ROOT_DS4621="/project/<group>/datasets/openneuro/ds004621"   # OpenNeuro ds004621 root
export DS4504_RESULTS_DIR="/scratch/$USER/amica_ds004504_v3"             # per-subject JSONs (AMICA + comparators)
export DS4621_RESULTS_DIR="/scratch/$USER/amica_ds004621_v3"

# ── Held-out / cross-validated MIR (consumed by submit_heldout_*.sh) ──
# 5-fold contiguous-block CV; per-subject JSON of train/held-out MIR. Default dir shown.
export HELDOUT_RESULTS_DIR="/scratch/$USER/amica_heldout_cv"

# ── Reproducibility strictness (paper campaigns) ──
# Set to 1 to make check_env.py verify FATAL on numerical-stack drift vs the
# committed publication lock (benchmark/cc_benchmark/locks/published/<venv>.lock.json)
# instead of a warning — so a rebuilt env that resolved a different torch/jax/numpy
# cannot silently produce "reproduced" numbers. Leave unset for dev iteration.
# export AMICA_STRICT_STACK=1

# ── Run configuration (read by run_one_subject.py via the submit scripts) ──
export AMICA_INPUT_LEVEL="bids"     # ds004505 input level
export AMICA_N_ITER=3000            # AMICA iterations
export AMICA_COMPUTE_DIPOLES=1      # 1 = compute dipolarity (slower), 0 = skip

# ── Cross-implementation MEMORY comparison (benchmark/comparator + submit_mem_*.sh) ──
# First build the competitors venv once:  bash setup_competitors.sh
export AMICA_MEM_DATASET="ds004505"   # 'mne_sample' (zero-setup, any user) or 'ds004505' (paper)
export AMICA_MEM_SUBJECT=1            # subject id (ds004505); ignored for mne_sample
export AMICA_MEM_NCOMP=64             # PCA components
export AMICA_MEM_ITER=100             # iterations (memory is iteration-independent; ~100 captures the peak)
export AMICA_MEM_CHUNK="auto"         # AMICA chunked-run chunk_size: 'auto' or an integer (frugal end of the dial)
# export AMICA17_BIN="/path/to/amica17"   # OPTIONAL: set to include Fortran AMICA 1.7 (you must build amica17)
# export AMICA_MEM_NVML=1                  # OPTIONAL GPU NVML cross-check (needs pynvml in both venvs)
